## 🔐 Password Generator (Python)

### 📌 Project Description

This is a **Python-based password generator** that creates strong and customizable passwords based on user preferences. The application allows users to choose the password length and decide whether to include uppercase letters, digits, and special symbols. Lowercase letters are included by default to ensure basic password strength.

### 🚀 Features

* User-defined password length
* Option to include **uppercase letters**, **digits**, and **special symbols**
* Lowercase letters included by default
* Random and secure password generation
* Simple command-line interface

### 🛠️ Technologies Used

* Python
* `random` module
* `string` module

### ⚙️ How It Works

* Prompts the user to enter the desired password length
* Asks the user whether to include uppercase letters, numbers, and special characters
* Combines selected character sets with lowercase letters
* Uses the `random` module to generate and display a secure password

### 📂 Use Case

Helps understand Python modules, user input handling, randomness, and basic password security principles.

### ▶️ How to Run

1. Clone the repository
2. Run the Python file:

   python password_generator.py
   
